#include <iostream>
#include <algorithm>
#include <cmath>
#include <stack>
#include <queue>
#include <cctype>
#include <vector>
#define FASTIO ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
using namespace std;

vector<int> times;

int immigration(vector<int> &time){

}

int main() {
    FASTIO;

    int n, m; cin >> n >> m;
    
    for(int i = 0; i < n; i++){
        int x; cin >> x;
        times.push_back(x);
    }
    return 0;
}
